# 추론 서버 런타임 의존성 (학습 전용 패키지는 제외).
# torch / torchaudio 의 +cu128 빌드는 호스트 CUDA 환경에 맞춰 별도 인덱스에서 가져온다.
# 신규 환경이라면 다음 순서로 설치한다:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu128 -r requirements.txt

fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.26
soundfile==0.13.1
numpy==2.4.4
torch==2.7.0+cu128
torchaudio==2.7.0+cu128
torchcodec==0.11.0
gTTS==2.5.4

# CMU Pronouncing Dictionary 기반 G2P. 사전 미등재 단어는 내장 신경망 폴백으로 처리.
g2p-en==2.1.0
